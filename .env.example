PORT=4173

# 2GIS official API key.
# Get it from the 2GIS developer console and keep it only on the backend.
TWO_GIS_API_KEY=

# Optional AI provider.
# If absent, backend uses a local template generator so the app still works.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
